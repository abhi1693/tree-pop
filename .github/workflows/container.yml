name: Container
on:
  release:
    types:
      - published
permissions:
  contents: read
  packages: write
jobs:
  images:
    uses: abhi1693/actions/.github/workflows/container-images.yml@v1
    with:
      manifest: .github/images.json
      publish: true
    permissions:
      contents: read
      packages: write
      id-token: write
      attestations: write
